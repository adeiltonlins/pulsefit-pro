import React,{useState} from 'react';
import {api,ApiUser} from '../../api';

export const ForcePasswordChangeModal:React.FC<{user:ApiUser;onChanged:(u:ApiUser)=>void}>=({user,onChanged})=>{
 const [currentPassword,setCurrentPassword]=useState(''),[newPassword,setNewPassword]=useState(''),[confirmPassword,setConfirmPassword]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);
 const save=async()=>{setMsg('');if(newPassword.length<12){setMsg('A nova senha precisa ter pelo menos 12 caracteres.');return}if(newPassword!==confirmPassword){setMsg('As novas senhas não coincidem.');return}setBusy(true);try{await api.changePassword(currentPassword,newPassword);onChanged({...user,mustChangePassword:false})}catch(e:any){setMsg(e.message||'Não foi possível alterar a senha.')}finally{setBusy(false)}};
 return <div className="fixed inset-0 z-[100] bg-black/90 grid place-items-center p-4"><div className="w-full max-w-md bg-[#121414] border-2 border-[#DFFF00] p-6 space-y-4"><div><div className="text-[10px] font-mono text-[#DFFF00]">// PRIMEIRO ACESSO</div><h2 className="font-anybody text-2xl font-black uppercase text-white">Crie sua nova senha</h2><p className="text-xs text-gray-400 mt-2">Por segurança, a senha temporária precisa ser substituída antes de usar o PulseFit.</p></div>{msg&&<div className="border border-red-500/40 p-3 text-xs text-red-300">{msg}</div>}<input autoFocus type="password" value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)} placeholder="Senha temporária atual" className="w-full bg-black border border-[#333] p-3 text-sm text-white outline-none focus:border-[#DFFF00]"/><input type="password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} placeholder="Nova senha (mínimo 12 caracteres)" className="w-full bg-black border border-[#333] p-3 text-sm text-white outline-none focus:border-[#DFFF00]"/><input type="password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} placeholder="Confirmar nova senha" className="w-full bg-black border border-[#333] p-3 text-sm text-white outline-none focus:border-[#DFFF00]"/><button disabled={busy} onClick={save} className="w-full py-3 bg-[#DFFF00] text-black font-anybody font-black uppercase disabled:opacity-50">{busy?'ALTERANDO...':'ALTERAR SENHA E CONTINUAR'}</button></div></div>;
};
