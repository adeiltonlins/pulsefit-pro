import React,{useEffect,useState} from 'react'; import {ScreenView,UserRole} from '../../types'; import {api} from '../../api';
export const AssinaturaScreen:React.FC<{onNavigate:(s:ScreenView)=>void;userRole?:UserRole}>=()=>{const [payments,setPayments]=useState<any[]>([]),[error,setError]=useState('');useEffect(()=>{api.payments().then(r=>setPayments(r.payments||[])).catch((e:any)=>setError(e.message))},[]);const pending=payments.filter(p=>p.status!=='paid').reduce((a,p)=>a+Number(p.amountCents||0),0)/100;return <div className="space-y-6">{error&&<div className="border border-red-500/40 p-3 text-red-300 text-xs">{error}</div>}<div><div className="text-[10px] font-mono text-[#DFFF00]">// FINANCEIRO</div><h1 className="font-anybody text-3xl font-black uppercase text-white">Cobranças & Assinatura</h1></div><div className="grid sm:grid-cols-3 gap-3"><div className="bg-[#121414] border border-[#333] p-5"><div className="text-[10px] text-gray-500 font-mono">LANÇAMENTOS</div><b className="text-3xl text-white">{payments.length}</b></div><div className="bg-[#121414] border border-[#333] p-5"><div className="text-[10px] text-gray-500 font-mono">PENDENTE</div><b className="text-3xl text-[#DFFF00]">R$ {pending.toFixed(2).replace('.',',')}</b></div><div className="bg-[#121414] border border-[#333] p-5"><div className="text-[10px] text-gray-500 font-mono">STATUS</div><b className="text-xl text-white">DADOS REAIS</b></div></div><div className="bg-[#121414] border border-[#333] p-5 space-y-2">{payments.length===0?<div className="text-xs font-mono text-gray-500">Nenhuma cobrança cadastrada.</div>:payments.map(p=><div key={p.id} className="grid grid-cols-2 sm:grid-cols-5 gap-2 items-center border-b border-[#222] py-3 text-xs font-mono"><span>{p.studentName||'Minha cobrança'}</span><span>{p.description||'Mensalidade'}</span><span>R$ {(Number(p.amountCents)/100).toFixed(2).replace('.',',')}</span><span>{p.dueDate}</span><span className={p.status==='paid'?'text-green-400':'text-[#DFFF00]'}>{p.status==='paid'?'PAGO':'PENDENTE'}</span></div>)}</div></div>}
