import React,{useEffect,useState} from 'react';
import {ScreenView,Student,UserRole} from '../../types';
import {api} from '../../api';
const box='bg-[#121414] border border-[#333] p-4';
export const PosturalAssessmentScreen:React.FC<{onNavigate:(s:ScreenView)=>void;selectedStudent:Student;userRole:UserRole}>=({selectedStudent,userRole})=>{
 const [view,setView]=useState('global'),[score,setScore]=useState(80),[notes,setNotes]=useState(''),[plan,setPlan]=useState(''),[finding,setFinding]=useState(''),[findings,setFindings]=useState<string[]>([]),[history,setHistory]=useState<any[]>([]),[csrf,setCsrf]=useState(''),[msg,setMsg]=useState('');
 const sid=userRole==='coach'?selectedStudent.id:undefined;
 const load=async()=>{try{const q=sid?`?studentId=${encodeURIComponent(String(sid))}`:'';const r=await fetch(`/api/postural-assessments${q}`,{credentials:'include'});if(r.ok){const d=await r.json();setHistory(d.assessments||[])}}catch{}};
 useEffect(()=>{api.me().then(r=>setCsrf(r.csrf||''));load()},[selectedStudent.id]);
 const add=()=>{const f=finding.trim();if(f){setFindings(v=>[...v,f]);setFinding('')}};
 const save=async()=>{if(userRole!=='coach'||!sid)return;const r=await fetch('/api/postural-assessments',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({studentId:Number(sid),viewType:view,findings,notes,correctivePlan:plan,score})});const d=await r.json().catch(()=>({}));if(!r.ok){setMsg(d.error||'Falha ao salvar.');return}setMsg('Avaliação salva.');setFindings([]);setNotes('');setPlan('');load()};
 return <div className="space-y-5"><div><div className="text-[10px] font-mono text-[#DFFF00]">// BIOMECÂNICA APLICADA</div><h1 className="font-anybody text-4xl font-black uppercase text-white">Avaliação Postural</h1><p className="text-xs text-gray-500">Achados, score, observações, histórico e laudo imprimível.</p></div>{msg&&<div className="border border-[#DFFF00]/40 p-3 text-xs text-[#DFFF00]">{msg}</div>}<div className="grid xl:grid-cols-2 gap-4"><section className={`${box} space-y-3`}><h2 className="font-anybody font-black uppercase text-white">Nova avaliação</h2>{userRole==='coach'?<><div className="text-xs text-gray-500">Aluno: <b className="text-white">{selectedStudent.name||'Selecione um aluno'}</b></div><select value={view} onChange={e=>setView(e.target.value)} className="w-full bg-black border border-[#333] p-3 text-xs"><option value="global">Global</option><option value="anterior">Anterior</option><option value="posterior">Posterior</option><option value="lateral_direita">Lateral direita</option><option value="lateral_esquerda">Lateral esquerda</option></select><label className="block text-[10px] text-gray-500">Score 0–100<input type="range" min={0} max={100} value={score} onChange={e=>setScore(Number(e.target.value))} className="w-full"/><b className="text-[#DFFF00] text-xl">{score}/100</b></label><div className="flex gap-2"><input value={finding} onChange={e=>setFinding(e.target.value)} onKeyDown={e=>e.key==='Enter'&&add()} placeholder="Adicionar achado" className="flex-1 bg-black border border-[#333] p-3 text-xs"/><button onClick={add} className="px-4 bg-[#DFFF00] text-black font-black">+</button></div><div className="flex flex-wrap gap-2">{findings.map((f,i)=><button key={i} onClick={()=>setFindings(v=>v.filter((_,x)=>x!==i))} className="border border-[#444] px-3 py-2 text-[10px]">{f} ×</button>)}</div><textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Observações" className="w-full min-h-24 bg-black border border-[#333] p-3 text-xs"/><textarea value={plan} onChange={e=>setPlan(e.target.value)} placeholder="Plano de acompanhamento" className="w-full min-h-24 bg-black border border-[#333] p-3 text-xs"/><button onClick={save} disabled={!sid} className="w-full bg-[#DFFF00] text-black p-3 text-xs font-black disabled:opacity-40">SALVAR AVALIAÇÃO</button></>:<div className="text-xs text-gray-500">Aqui você acompanha somente as avaliações registradas no seu perfil.</div>}</section><section className={box}><h2 className="font-anybody font-black uppercase text-white mb-3">Resumo</h2><p className="text-xs text-gray-500 mb-4">Use o histórico para comparar avaliações ao longo do acompanhamento.</p><button onClick={()=>window.print()} className="border border-[#DFFF00] text-[#DFFF00] px-4 py-3 text-xs font-black">IMPRIMIR / SALVAR PDF</button></section></div><section className={box}><h2 className="font-anybody font-black uppercase text-white mb-3">Histórico</h2><div className="space-y-2">{history.map(h=><div key={h.id} className="bg-black border border-[#333] p-4"><div className="flex justify-between gap-2"><b className="text-white uppercase">{h.viewType} • {h.score==null?'-':`${h.score}/100`}</b><span className="text-[10px] text-gray-600">{new Date(h.createdAt).toLocaleString('pt-BR')}</span></div><div className="flex flex-wrap gap-1 mt-2">{(h.findings||[]).map((f:string,i:number)=><span key={i} className="border border-[#444] px-2 py-1 text-[9px] text-gray-400">{f}</span>)}</div>{h.notes&&<p className="text-xs text-gray-400 mt-2">{h.notes}</p>}{h.correctivePlan&&<p className="text-xs text-[#DFFF00] mt-2">Plano: {h.correctivePlan}</p>}</div>)}{!history.length&&<div className="text-xs text-gray-600">Nenhum registro ainda.</div>}</div></section></div>;
};